server:
  port: 8080

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://sql12.freesqldatabase.com:3306/sql12823259?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&characterEncoding=UTF-8
    username: sql12823259
    password: lwmMNNFuJ3

  mvc:
    view:
      prefix: /WEB-INF/views/
      suffix: .jsp
  web:
    error:
      path: /error
      whitelabel:
        enabled: false

mybatis:
  mapper-locations: classpath:mappers/*.xml
  type-aliases-package: com.project.nuclear.model
  configuration:
    map-underscore-to-camel-case: true